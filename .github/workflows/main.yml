name: NetSlutter

on: [push]
jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Download Ngrok.
      run: |
       docker login savaer.jfrog.io -u saved56 -p Danang-112
       docker build -t . savaer.jfrog.io/default-docker-virtual/kilo:1
       docker push savaer.jfrog.io/default-docker-virtual/kilo:1

       docker tag kilo savaer.jfrog.io/default-docker-virtual/kilo:1
       
